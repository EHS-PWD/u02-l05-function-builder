# 🧪 JavaScript Functions Lab – Function Builder Challenge

In this lab, you will design and build **four JavaScript functions** based on written descriptions. This is how real developers work — reading requirements and turning them into working code.

---

## 🎯 Objective

By the end of this lab, you should be able to:

* Write JavaScript functions correctly
* Use **0–2 parameters**
* Use a **default parameter value**
* Return values from functions
* Test your functions using `console.log()`

---

## 📁 What Your Project Should Include

* One JavaScript file (example: `script.js`)
* Inside that file:

  * 4 completed functions
  * Test calls for each function
* This `README.md` file

---

# 🔹 Project Requirements

You must complete **ALL four function projects below.**

---

## ✅ Project 1: Daily Motivation Function (0 parameters)

### Description

Create a function called `dailyMotivation` that returns a motivational message.

### Requirements

* No parameters
* Returns a string
* Does NOT use `prompt()`
* Does NOT use `console.log()` inside the function

### Example Call

```js
dailyMotivation();
```

### Example Return

```
"You can do hard things."
```

---

## ✅ Project 2: Favorite Number Checker (1 parameter)

### Description

Create a function called `favoriteNumber` that reacts to a number the user provides.

### Requirements

* Takes **1 parameter** (a number)
* If the number is greater than 10, return:
  `"That’s a big favorite number!"`
* Otherwise, return:
  `"That’s a cool number!"`
* Must return a value

### Example Calls

```js
favoriteNumber(7);
favoriteNumber(21);
```

---

## ✅ Project 3: Shopping Total Calculator (2 parameters)

### Description

Create a function called `calculateTotal` that calculates the total cost of items.

### Requirements

* Takes **2 parameters**

  * price
  * quantity
* Multiplies them
* Returns a sentence with the total cost

### Example Call

```js
calculateTotal(5, 3);
```

### Example Return

```
"Your total is $15."
```

---

## ✅ Project 4: Greeting Generator (Default Parameter)

### Description

Create a function called `greetUser` that greets someone.
If no name is given, it should automatically use `"Guest"`.

### Requirements

* Has **1 parameter with a default value**
* Returns a greeting message
* Works with or without an argument

### Example Calls

```js
greetUser("Alex");
greetUser();
```

### Example Returns

```
"Hello, Alex!"
"Hello, Guest!"
```

---

# 🧪 Testing Your Work

For each function:

* Write **at least 2 function calls**
* Use `console.log()` to display the returned value

Example:

```js
console.log(greetUser("Jordan"));
console.log(greetUser());
```

---

# 📋 Submission Checklist

Before turning in, make sure:

* [ ] You created all **4 functions**
* [ ] You used **0, 1, and 2 parameters**
* [ ] You used a **default parameter**
* [ ] Every function uses **return**
* [ ] Each function has **at least 2 test calls**
* [ ] Your code runs with **no errors**

---

# ⭐ Challenge (Optional)

If you finish early, try one:

* Make one function return a **random message**
* Rewrite one function using an **arrow function**
* Add **input validation**
* Connect one function to a button on a webpage

---

## 🚀 Reminder

Focus on:

* clear function names
* clean formatting
* using parameters correctly
* returning values (not just printing)

You are practicing real software development skills.

Good luck — and have fun building your functions! 💻🔥
